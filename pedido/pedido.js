(()=>{const p=new URLSearchParams(location.search),reference=p.get("referencia"),token=p.get("token"),root=document.getElementById("order"),money=new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}),clean=v=>String(v||"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));async function load(){const{data,error}=await supabaseClient.rpc("public_get_menu_order",{target_reference:reference,target_token:token});if(error||!data){root.innerHTML='<section class="public-hero"><h1>Pedido indisponível</h1><p>Confira o link recebido.</p></section>';return}root.innerHTML=`<section class="public-hero"><small>PEDIDO ${clean(data.reference)}</small><h1>${clean(data.status)}</h1><p>${clean(data.customer_name)} · ${clean(data.fulfillment_type)}</p></section><article class="public-card">${data.items.map(i=>`<div class="menu-price-row"><span>${i.quantity}x ${clean(i.name)} ${clean(i.label)}</span><strong>${money.format(i.line_total)}</strong></div>`).join("")}<hr><div class="menu-price-row"><strong>Total</strong><strong class="proposal-total">${money.format(data.total_amount)}</strong></div></article>`}load()})();

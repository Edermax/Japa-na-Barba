(()=>{const p=new URLSearchParams(location.search),reference=p.get("referencia"),token=p.get("token"),root=document.getElementById("proposal"),money=new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}),clean=v=>String(v||"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));async function load(){const{data,error}=await supabaseClient.rpc("public_get_quote_proposal",{target_reference:reference,target_token:token});if(error||!data){root.innerHTML='<section class="public-hero"><h1>Proposta indisponível</h1><p>O link pode estar incorreto ou expirado.</p></section>';return}const x=data.proposal;root.innerHTML=`<section class="public-hero"><small>PROPOSTA ${clean(data.reference)}</small><h1>${clean(x.title)}</h1><p>${clean(x.introduction)}</p></section><article class="public-card"><h2>Escopo</h2><p>${clean(x.scope)}</p>${data.items.map(i=>`<div class="menu-price-row"><span>${clean(i.description)} · ${i.quantity}x</span><strong>${money.format(i.line_total)}</strong></div>`).join("")}<hr><div class="menu-price-row"><strong>Total</strong><strong class="proposal-total">${money.format(x.total_amount)}</strong></div><p>${clean(x.terms)}</p><p id="answer" class="public-message"></p><div class="commercial-actions" id="actions"><button class="public-button" data-answer="accepted">Aceitar proposta</button><button class="public-button secondary" data-answer="rejected">Recusar</button></div></article>`;if(!["sent","viewed"].includes(x.status))document.getElementById("actions").innerHTML=`<span class="commercial-badge">${clean(x.status)}</span>`;root.querySelectorAll("[data-answer]").forEach(b=>b.addEventListener("click",()=>respond(b.dataset.answer)))}async function respond(decision){const{data,error}=await supabaseClient.rpc("public_respond_quote_proposal",{target_reference:reference,target_token:token,target_decision:decision});document.getElementById("answer").textContent=error?error.message:(data?"Resposta registrada com sucesso.":"Não foi possível registrar a resposta.");if(data)load()}load()})();
